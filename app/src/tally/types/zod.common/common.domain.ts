export enum ConnectionState {
  DISABLED = "Disabled",
  OFFLINE = "Offline",
  ONLINE = "Online",
  ERROR = "Error",
  FAILED = "Failed",
}

export enum TallyState {
  NONE = 0,
  DANGER = 4,
  INFO = 8,
  WARNING = 12,
  LIGHT = 14,
  PREVIEW = 16,
  PROGRAM = 20,
}
