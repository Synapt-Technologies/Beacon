IMG_NAME="Beacon"
RELEASE="bookworm"
DEPLOY_COMPRESSION=gz

LOCALE_DEFAULT="en_US.UTF-8"
TARGET_HOSTNAME="beacon"
KEYBOARD_KEYMAP="us"
KEYBOARD_LAYOUT="English (US)"
TIMEZONE_DEFAULT="Europe/Amsterdam"

FIRST_USER_NAME="beacon"
FIRST_USER_PASS="beacon"
ENABLE_SSH=1

# Only build stages we need — skip the desktop stages (3/4/5)
STAGE_LIST="stage0 stage1 stage2 stage-beacon"
